var score1 = 90;     // Round 1 score
var score2 = 95;     // Round 2 score
var highScore1 = 75; // Round 1 high score
var highScore2 = 95; // Round 2 high score

// Check if scores are higher than current high scores
//Let's break this down. We want to check for an output Should we save outputs in new ////variables for future use?

//Create a variable named comparison 
//We want to compare 90 + 95 > 75 + 95; But we don't want to use the actual number values. 
// Lets replace the number values with the variable names bound to those number values. 
//kp demo this
var comparison = (score1 + score2) > (highScore1 + highScore2);





// Write the message into the page
// var el = document.getElementById('answer');
// el.innerHTML = 'New high score: ' + comparison;